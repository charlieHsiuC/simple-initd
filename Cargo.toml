[package]
name = "simple-initd"
version = "0.1.0"
edition = "2024"

[dependencies]
rustix = { version = "1.1.4", default-features = false, features=["mount"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"